<!DOCTYPE html>
<html lang="id" style="scroll-behavior: smooth;">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-QWTKZyjpPEjISv5WaRU9OFeRpok6YctnYmDr5pNlyT2bRjXh0JMhjY6hW+ALEwIH" crossorigin="anonymous">
    <link rel="stylesheet" href="/css/custom.css">
    
    <title><%= locals.title || 'CureCart' %></title>
</head>
<body class="bg-light"> 

<nav class="navbar navbar-expand-lg navbar-light bg-white shadow-sm sticky-top">
    <div class="container">
        
        <a class="navbar-brand fw-bold text-dark" href="/">CureCart Shop</a>
        
        <a href="#" class="nav-link text-secondary d-none d-lg-block ms-2">Kategori</a>
        
        <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarContent" aria-controls="navbarContent" aria-expanded="false" aria-label="Toggle navigation">
            <span class="navbar-toggler-icon"></span>
        </button>

        <div class="collapse navbar-collapse" id="navbarContent"> 
            
            <form action="/" method="GET" class="d-flex navbar-search-form mx-auto w-50 my-2 my-lg-0">
                <input 
                    class="form-control" 
                    type="search" 
                    placeholder="Cari di CureCart Shop..." 
                    name="search" 
                    aria-label="Search"
                    value="<%= locals.searchTerm || '' %>" 
                >
            </form>

            <ul class="navbar-nav ms-auto mb-2 mb-lg-0 align-items-center">
                
                <% if (user) { %>
                    <% if (!user.is_admin) { %>
                        <li class="nav-item me-3">
                            <a class="nav-link position-relative" href="/cart" title="Keranjang">
                                <svg xmlns="http://www.w3.org/2000/svg" width="22" height="22" fill="currentColor" class="bi bi-cart" viewBox="0 0 16 16">
                                    <path d="M0 1.5A.5.5 0 0 1 .5 1H2a.5.5 0 0 1 .485.379L2.89 3H14.5a.5.5 0 0 1 .491.592l-1.5 8A.5.5 0 0 1 13 12H4a.5.5 0 0 1-.491-.408L2.01 3.607 1.61 2H.5a.5.5 0 0 1-.5-.5zM5 12a2 2 0 1 0 0 4 2 2 0 0 0 0-4zm7 0a2 2 0 1 0 0 4 2 2 0 0 0 0-4zm-7 1a1 1 0 1 1 0 2 1 1 0 0 1 0-2zm7 0a1 1 0 1 1 0 2 1 1 0 0 1 0-2z"/>
                                </svg>
                                <span class="d-lg-none ms-2">Keranjang</span>
                            </a>
                        </li>
                    <% } %>
                    
                    <% if (user.is_admin) { %>
                        <li class="nav-item">
                            <a class="nav-link" href="/admin/orders">Pesanan</a>
                        </li>
                        <li class="nav-item">
                            <a class="nav-link" href="/admin/products">Produk</a>
                        </li>
                    <% } %>

                    <li class="nav-item dropdown ms-2">
                        <a class="nav-link dropdown-toggle d-flex align-items-center" href="#" id="navbarDropdown" role="button" data-bs-toggle="dropdown" aria-expanded="false">
                            <img src="https://ui-avatars.com/api/?name=<%= user.name %>&background=random" alt="<%= user.name %>" class="rounded-circle me-2" width="32" height="32">
                            <span class="d-none d-lg-inline text-dark fw-bold"><%= user.name %></span>
                        </a>
                        <ul class="dropdown-menu dropdown-menu-end shadow border-0 mt-2" aria-labelledby="navbarDropdown">
                            <li><a class="dropdown-item" href="#">Halo, <%= user.name %>!</a></li>
                            <% if (!user.is_admin) { %>
                                <li><a class="dropdown-item" href="/my-orders">Daftar Transaksi</a></li>
                            <% } %>
                            <li><hr class="dropdown-divider"></li>
                            <li>
                                <form action="/logout" method="POST" class="d-inline">
                                    <button type="submit" class="dropdown-item text-danger">Keluar</button>
                                </form>
                            </li>
                        </ul>
                    </li>

                <% } else { %>
                    <li class="nav-item me-2">
                        <a class="nav-link btn btn-outline-orchid px-3 fw-bold" href="/login">Masuk</a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link btn btn-orchid text-white px-3 fw-bold" href="/register">Daftar</a>
                    </li>
                <% } %>
            </ul>
        </div>
    </div>
</nav>

<main class="mt-4">